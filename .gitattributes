# 行尾统一：仓库里一律存 LF，工作区也检成 LF（避免每次提交都刷 CRLF 警告）
* text=auto eol=lf

# Windows 专有脚本必须 CRLF —— cmd.exe 不认纯 LF 的批处理（实测过）
*.cmd text eol=crlf
*.bat text eol=crlf
*.ps1 text eol=crlf

# 二进制：别当文本转换
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.db binary

# 学生档案

> 这是学生自己的文件：改完保存，下次对话生效。AI 每次会话都会自动读到它
> （已在 `kilo.json` 的 `instructions` 里登记），不需要在对话里重复交代。

## 一、讲解风格（只改冒号后面的词，候选值照抄）

```
互动频率: 标准        # 少互动 | 标准 | 多互动 | 高频互动
信息密度: 标准        # 极简 | 标准 | 详尽 | 深入
引导强度: 标准        # 直接给解析 | 标准 | 引导为主 | 只提问
```

## 二、归档积极程度（三条独立设置，互不影响）

```
错题本积极程度: 标准    # 不主动 | 标准 | 主动 | 直接记
背诵本积极程度: 标准    # 不主动 | 标准 | 主动 | 直接记
笔记本积极程度: 标准    # 不主动 | 标准 | 主动 | 直接记
```

- `不主动`：你不说就不记。`标准`：讲完问你一句。`主动`：问的时候把要记的内容先拟好，你点头就写。
- `直接记`：**不问你，也不提示，直接写入**——完全无感，顺手就记好了（你在编辑器里能看到 AI 改了什么）。
  要撤销就说"把刚才记的那条删掉"。
- 你用 `/wrong`、`/recite`、`/note` 明确要求归档时，无论这里怎么设都**直接写入**（这几种情况会回声字段供你纠正）。

每个档位具体怎么讲、怎么归档，见 `.kilo/skills/tutor/SKILL.md`。

## 三、自由备注（最多 3 行，用来写特例）

- （示例：英语背单词用极简；周六只想刷错题，不想听课）

## 四、称呼与学情（可留空）

- 称呼：同学
- 年级/进度：
- 薄弱点：

> 也可以不自己改文件，直接跟 AI 说"以后都讲详细点"——它会把上面的档位改好并回报。
> 单次要求（"这题讲细点"）只作用于那一次，不会写进这里。
